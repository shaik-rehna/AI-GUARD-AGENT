# Core dependencies
opencv-python>=4.5.0
face-recognition>=1.3.0
numpy>=1.21.0
SpeechRecognition>=3.8.0
pywin32>=300; sys_platform == 'win32'
Pillow>=8.3.0  # Required by face-recognition

# "pip install -r requirements.txt" to install the requirements